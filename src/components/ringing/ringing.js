import React from 'react'

// TODO: add mutation for answer and decline call
// TODO: add buttons for these cases
// TODO: add subscription for incoming call => if accept => change global state isCalling.number and isCalling.direction

const Ringing = () => {
  return <p>ringing</p>
}

export default Ringing
